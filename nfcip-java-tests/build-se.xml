<?xml version="1.0"?>
<project name="nfcip-java-tests-se" default="compile" basedir=".">
	<description>Test Classes for the NFCIP Java library</description>
	<property name="version" value="1.3.0" />

	<!-- set global properties for this build -->
	<property name="src" location="src" />
	<property name="build" location="build" />
	<property name="dist" location="dist" />
	<property name="lib" location="lib" />

	<!-- this file contains the locations of the required libraries -->
	<property file="${lib}/lib.properties" />

	<path id="compile.classpath">
		<pathelement location="${nfcip-java-se.jar}" />
	</path>

	<!-- init -->
	<target name="init">
		<!-- Create the time stamp -->
		<tstamp />
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}" />
	</target>

	<!-- compile -->
	<target name="compile" depends="init" description="compile the source">
		<javac srcdir="${src}"
		       destdir="${build}"
		       includes="ds/nfcip/tests/se/**"
		       classpathref="compile.classpath" />
	</target>

	<!-- dist -->
	<target name="dist"
	        depends="compile"
	        description="generate the distribution">
		<mkdir dir="${dist}" />
		<jar jarfile="${dist}/${ant.project.name}-${version}.jar"
		     basedir="${build}" />
	</target>

	<!-- clean -->
	<target name="clean" description="clean up">
		<delete dir="${build}" />
		<delete dir="${dist}" />
	</target>
</project>
